{
  "name": "ttfx-node",
  "version": "0.1.0",
  "private": true,
  "description": "Headless ttfx frames for JS CLI builders (napi binding, sync)",
  "license": "MIT",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2"
  }
}
